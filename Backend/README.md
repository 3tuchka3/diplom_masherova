# Система управления предприятием (Backend)

Программный комплекс на базе Django REST Framework для автоматизации учета на КПП, складе ковровых изделий и в общежитии предприятия.

## 🛠 Технологический стек
* **Framework:** Django 4.2 + Django REST Framework
  * **Auth:** SimpleJWT (JSON Web Token)
  * **Database:** PostgreSQL
  * **Environment:** PyCharm

## 🔐 Ролевая модель и безопасность
Доступ к API ограничен на основе ролей пользователей. Права проверяются на уровне бэкенда при каждом запросе.

### Существующие роли:
1. **Администратор КПП (`is_checkpoint_officer`)** — доступ к журналу въезда/выезда транспорта.
   2. **Администратор склада (`is_carpet_admin`)** — управление каталогом ковровых изделий.
   3. **Менеджер общежития (`is_dorm_manager`)** — учет жильцов и контроль коммунальных платежей.
   4. **Суперпользователь** — полный доступ к админ-панели и управлению ролями.

### Процесс активации пользователя:
1. Регистрация через эндпоинт `/api/accounts/register/`. 
   2. По умолчанию аккаунт **неактивен** (`is_active=False`).
   3. Администратор в панели управления одобряет заявку, активирует пользователя и назначает соответствующую роль.

## 📡 Основные API Эндпоинты

### Авторизация
* `POST /api/accounts/login/` — Вход (получение Access и Refresh токенов).
  * `POST /api/accounts/register/` — Регистрация нового сотрудника.

### Модуль КПП
* `GET /api/checkpoint/records/` — Список всех записей транспорта.
  * `POST /api/checkpoint/records/` — Регистрация въезда.
  * `POST /api/checkpoint/records/{id}/exit/` — Регистрация выезда (автоматическая смена статуса).

### Модуль Склада (Ковры)
* `GET /api/carpets/items/` — Полный каталог изделий.

### Модуль Общежития
* `GET /api/dormitory/residents/` — Список проживающих.
  * `GET /api/dormitory/bills/` — Учет коммунальных счетов.

## 🚀 Запуск проекта
1. Установить зависимости: `pip install -r requirements.txt`
   2. Выполнить миграции: `python manage.py migrate`
   3. Создать суперпользователя: `python manage.py createsuperuser`
   4. Запустить сервер: `python manage.py runserver`

---
*Проект разработан в рамках дипломной работы. 2026 г.*